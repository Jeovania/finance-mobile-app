import { View } from 'react-native'
import styled from 'styled-components/native'
import { space, color, flexbox, position, border } from 'styled-system'

export const StyledContainer = styled(View)`
    ${space}
    ${color}
    ${border}
    ${flexbox}
    ${position}
  `
